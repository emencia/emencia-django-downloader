"""Test setting for emencia.django.emencia.django.downloader"""
import os

DATABASE_ENGINE = 'sqlite3'
DATABASE_NAME = '/tmp/emencia.django.downloader.db'
INSTALLED_APPS = ['emencia.django.emencia.django.downloader',]

